# About Me Feature - Інструкція

## Що було зроблено

Я успішно перенесла верстку сторінки "About Me" з HTML шаблону (`public/template/backend/about.html`) в Vue компонент з використанням Inertia.js та UI компонентів проекту.

## Створені/Оновлені файли

### 1. **Vue компонент** - `resources/js/pages/Abouts/Index.vue`
Повнофункціональна форма для редагування інформації "About Me" з:
- Полями для імені, email, телефону, адреси
- Текстовими полями для опису, резюме та tagline
- Завантаженням Home Image та About Image (banner)
- Завантаженням CV у форматі PDF
- Валідацією форми та відображенням помилок
- Попереднім переглядом зображень
- Інтеграцією з Inertia.js форм хелпером

### 2. **UI компонент Textarea** - `resources/js/components/ui/textarea/`
Створено новий UI компонент:
- `Textarea.vue` - компонент з підтримкою v-model
- `index.ts` - експорт компонента
- Стиль відповідає іншим UI компонентам проекту

### 3. **Controller** - `app/Http/Controllers/AboutController.php`
Оновлено методи:
- `index()` - відображення форми з існуючими даними
- `store()` - створення нового запису з валідацією та завантаженням файлів
- `update()` - оновлення існуючого запису

### 4. **Routes** - `routes/web.php`
Додано middleware `auth` та `verified` для захисту адміністративних маршрутів.

## Структура бази даних

Таблиця `abouts` має наступні поля:
- `name` - повне ім'я
- `email` - email адреса
- `phone` - телефон
- `address` - адреса
- `description` - детальний опис
- `summary` - короткий опис
- `tagline` - слоган
- `home_image` - зображення для головної сторінки
- `banner_image` - зображення для сторінки About
- `cv` - файл CV (PDF)

## Як користуватися

### 1. Запуск серверів

```bash
# Terminal 1 - Laravel сервер
php artisan serve

# Terminal 2 - Vite dev server
npm run dev
```

### 2. Доступ до сторінки

Після авторизації перейдіть на:
```
http://localhost:8000/abouts
```

### 3. Робота з формою

1. **Заповніть поля** - введіть всю необхідну інформацію
2. **Завантажте зображення** - виберіть файли для Home Image та About Image
3. **Завантажте CV** - виберіть PDF файл вашого резюме
4. **Натисніть "Update Changes"** - для збереження даних

### 4. Перегляд завантажених файлів

Файли зберігаються в:
- Зображення: `storage/app/public/images/about/`
- CV: `storage/app/public/cv/`

Доступ через URL:
- `http://localhost:8000/storage/images/about/filename.jpg`
- `http://localhost:8000/storage/cv/filename.pdf`

## Важливі примітки

### Storage Link
Переконайтеся, що symbolic link створено:
```bash
php artisan storage:link
```

### Права доступу
Перевірте права на директорії:
```bash
chmod -R 775 storage/
chmod -R 775 bootstrap/cache/
```

### Валідація файлів
- Зображення: максимум 2MB, формати: jpg, jpeg, png, gif, svg
- CV: максимум 5MB, тільки PDF формат

## Функціонал

✅ Відображення існуючих даних  
✅ Створення нового запису  
✅ Оновлення існуючого запису  
✅ Завантаження файлів (зображення та PDF)  
✅ Попередній перегляд зображень  
✅ Валідація форми  
✅ Відображення помилок валідації  
✅ Захист маршрутів через middleware  
✅ Респонсивний дизайн  
✅ Інтеграція з UI компонентами проекту  

## Можливості для розширення

- Додати можливість видалення старих файлів при завантаженні нових
- Додати обрізку та оптимізацію зображень
- Додати можливість перегляду CV прямо на сторінці
- Додати історію змін
- Додати багатомовність

## Приклад використання у коді

```typescript
// Імпорт маршрутів
import { store, update } from '@/routes/abouts';

// Отримання даних
const form = useForm({
    name: 'John Doe',
    email: 'john@example.com',
    // ... інші поля
});

// Відправка форми для створення
form.post(store().url, {
    forceFormData: true, // Важливо для файлів!
    preserveScroll: true,
});

// Відправка форми для оновлення
form.put(update(aboutId).url, {
    preserveScroll: true,
});
```

## Troubleshooting

### Помилка "Storage link not found"
```bash
php artisan storage:link
```

### Помилка "Permission denied"
```bash
sudo chown -R $USER:www-data storage/
sudo chmod -R 775 storage/
```

### Файли не завантажуються
Перевірте:
1. `php.ini` - `upload_max_filesize` та `post_max_size`
2. Права на директорії storage
3. Наявність symbolic link

---

Якщо виникнуть питання або потрібна додаткова функціональність, звертайтеся! 🚀

